Here is the minute, step-by-step implementation plan for Sprint 1.Phase 1: Environment & Core OrchestrationBefore writing any business logic, we need the local sandbox locked in.Spin up the LLM Infrastructure: Ensure Ollama is running locally. You will need to pull the specific models via your terminal: ollama pull qwen2.5-coder (for inference) and ollama pull nomic-embed-text (for embeddings).  Initialize the Backend: Set up a clean Python 3.11 environment.Install Core Dependencies: You will need fastapi, uvicorn (for the server), langchain, langchain-community, chromadb, gitpython, and sentence-transformers (for the reranker).  Phase 2: The Ingestion Service (Git & AST Chunking)This is where we turn a repository into logical, searchable pieces.The Git Clone Module: Write a Python function utilizing GitPython to accept a GitHub HTTPS URL and clone it into a temporary local directory (tempfile module is great for this).  File Traversal & Filtering: Create a walker function that recursively reads the cloned directory, ignoring binary files and .git folders.AST-Aware Chunking: Refer to the LangChain documentation for LanguageParser and RecursiveCharacterTextSplitter.from_language. You will want to map file extensions (e.g., .py, .js) to their respective splitters to preserve function and class boundaries.  Phase 3: Vectorization & StorageWhen mapping out the ChromaDB payload, you can actually adapt the same Python automation logic you recently built for enriching RAG schemas with JSON and CSV files to structure our chunk metadata here.Metadata Construction: For every chunk generated in Phase 2, append a metadata dictionary containing: repo_url, file_path, start_line, end_line, and language.  Initialize Embeddings: Instantiate LangChain's OllamaEmbeddings, passing model="nomic-embed-text".  ChromaDB Upsert: Initialize a persistent ChromaDB client (saving to a local ./chroma_db folder). Pass your document chunks, their enriched metadata, and the Ollama embedding function to Chroma.from_documents().Phase 4: Retrieval & LCEL Intent RoutingThis is the brain of the operation, ensuring your small local model isn't overwhelmed.The Intent Router: Using LangChain Expression Language (LCEL), write a function that scans the user prompt using a regex pattern. If it detects a file path (e.g., src/main.py), route to a retriever filtered by that specific file_path metadata. Otherwise, route to a global hybrid search.  BM25 Hybrid Search Integration: Look into the rank_bm25 library to create a sparse index alongside your ChromaDB dense index, and implement a Reciprocal Rank Fusion (RRF) function to merge the results.  Cross-Encoder Reranking: Use sentence-transformers to load cross-encoder/ms-marco-MiniLM-L-6-v2. Write a function that takes the top 20 retrieved chunks, scores them strictly against the user's prompt, and slices the array to the top 5 to fit your 4,096 token budget.  Phase 5: FastAPI Streaming EndpointThe Chat Endpoint: Create a POST /api/chat endpoint in FastAPI.  Prompt Construction: Assemble your system prompt. Crucially, instruct the LLM to format its references exactly as [FILE:path:Lstart-Lend]. Feed the reranked chunks and the user query into the prompt.  Server-Sent Events (SSE): Utilize FastAPI's StreamingResponse. Hook this up to LangChain's async stream method (astream) for the Ollama LLM to push tokens to the frontend in real-time.  Phase 6: React + Vite Split-Pane DashboardProject Init: Scaffold a React application using Vite.  UI Layout: Build the split-pane component. The left pane should render the code tree and utilize Prism.js for syntax highlighting.  SSE Consumption: On the right pane, set up an API fetch that reads the SSE stream.Citation Interceptor: Write a parser that monitors the incoming stream for the [FILE:...] regex pattern. When detected, trigger a state update that highlights the corresponding lines in the Prism.js viewer. 

these are the first sprint and the phases in that 
sso keep this convo saved 